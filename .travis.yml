sudo: required
services:
  - docker

before_install:
  - docker build -t phizercost/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run phizercost/react-test npm test -- --coverage

after_success:
  - docker build -t phizercost/multi-client ./client
  - docker build -t phizercost/multi-nginx ./nginx
  - docker build -t phizercost/multi-server ./server
  - docker build -t phizercost/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push phizercost/multi-client
  - docker push phizercost/multi-nginx
  - docker push phizercost/multi-server
  - docker push phizercost/multi-worker

